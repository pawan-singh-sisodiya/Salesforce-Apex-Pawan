import { getDataConnectorSourceFields } from 'lightning/analyticsWaveApi';
import { LightningElement } from 'lwc';

export default class MapRoutingEsri extends LightningElement {
    connectedCallback(){
     console.log('HI pawan');
    }
}